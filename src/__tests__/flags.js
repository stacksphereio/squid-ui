// src/__tests__/flags.js
// This is a placeholder stub for mocking feature flags in tests

export const flags = {
  betaBanner: true,
  flagDebugFooter: true,
  logoutButton: true,
};

describe('flags helper', () => {
  it('loads helper without errors', () => {
    expect(true).toBe(true);
  });
});